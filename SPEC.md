# 고립은둔청년 맞춤형 사회 복귀 AI 서비스 — 확정 명세

작성 기준일: 2026-08-14
상태: 확정 (변경 시 이 문서를 먼저 수정하고 전원에게 공유)

작업 규칙은 `CLAUDE.md`, 담당과 일정은 `업무분담.md`를 본다.

---

## 1. 프로젝트 제약

| 항목 | 내용 |
|---|---|
| 성격 | 부트캠프 프로젝트. 실사용자 없음, 데모·시연 목적 |
| 인원 | 5인. 전원 비개발자 수준, Claude Code로 구현. 디자인은 Figma AI |
| 예산 | AWS 크레딧 $1000. 비용 최적화는 심사 설명용이며 실제 부담은 없음 |
| 개발 기간 | 8/14 ~ 8/22 (실질 8일) |
| 피드백 | 8/23 ~ 8/26 현직자 피드백. **잔여 기능 완성을 우선**하며 치명적 지적만 반영 |
| 녹화 | 8/26. 기능별 클립을 개별 녹화한 뒤 편집 |
| 발표 | 8/28 |
| 심사 요건 | AWS로 24시간 동작하는 라이브 서비스 + 비용 최적화·트래픽·보안 관리 설명 |

시연은 발표 당일 라이브가 아니라 사전 녹화 영상으로 진행한다. 다만 서비스 자체는 발표 시점까지 계속 라이브 상태를 유지한다.

---

## 2. 유형과 종족

내부 유형 코드는 3종이며, 사용자 화면에는 유형명을 절대 노출하지 않는다. "건강·정서취약형" 같은 표현이 그대로 보이면 낙인이 되기 때문이다. 화면에는 종족과 동물 이름만 쓴다.

| 내부 유형 코드 | 과(표시용 종족) | 표시 동물 | 컬러 | 연구 근거 요약 |
|---|---|---|---|---|
| `HEALTH_EMOTION` | 개과 | 여우 | 노을 주황 `#E8956A` | 우울 57%, 소진 85%, 미취업 54%, 의료 미충족·활동 제약 높음 |
| `INDEPENDENT_LOW_INCOME` | 고양잇과 | 고양이 | 새벽 파랑 `#6A95C8` | 1인 가구 92%, 저소득 87%, 부채 31% |
| `FAMILY_LIVING` | 곰과 | 곰 | 이끼 초록 `#7AAE82` | 가족 동거, 건강 취약성 낮음, 미취업 43% |

출처: `(연구보고서_요약)_고립은둔청년의_삶은_어떻게_유형화될까요.pdf`

**동물·컬러는 2026-08-19에 교체했다.** 옛 값(여우 = 독립거주-저소득형 / 고양이 = 건강·정서취약형, 앰버 오렌지·라벤더 퍼플·세이지 그린)은 쓰지 않는다. hex와 컬러명의 유일한 출처는 `lib/types.ts`의 `TRIBE`이고, 같은 hex를 `styles/tokens.css`의 `[data-tribe]`가 들고 있다 — 한쪽만 고치지 않는다.

**컬러명은 첫 `npm run db:seed` 전에 확정한다.** `prisma/seed/items.ts`의 upsert가 아이템 `name`을 유니크 키로 쓰므로, 시드를 한 번 돌린 뒤 이름을 바꾸면 옛 행이 남고 새 행이 추가된다. 위 3개 이름을 확정값으로 본다.

**치장 아이템은 종족과 무관하다 (2026-08-20).** 치장에서 `tribeColor`를 지웠고(결정 변경 14번), 이어서 아이템 구성도 배경 6종(`배경1`~`배경6`)으로 줄였다. 이름에 컬러명이 없으므로 위 표의 종족색과 짝지어 읽을 것도 없다. 고양잇과 유저가 어떤 배경을 쓰든 정상이며, 종족 제한은 **스킨에만** 있다(5절).

### 표시명과 닉네임

진단이 끝나면 `형용사 + 동물` 형태의 기본 닉네임을 자동 부여한다. 예: "조용한 고양이", "부지런한 여우", "다정한 곰".

형용사는 4종이며 형용사 문항(`Q13` — "어떤 때가 가장 편한가요?") 4지선다 답에 1:1로 매핑한다. 이 문항은 지표를 켜지 않으므로 조기 종료와 무관하게 항상 마지막에 묻는다.

| `Q13` 선택지 | 형용사 |
|---|---|
| 밤에 혼자 있는 시간이 가장 편해요 | 조용한 |
| 마음 맞는 사람과 있을 때가 편해요 | 다정한 |
| 계획대로 하루가 굴러가면 편해요 | 부지런한 |
| 서두르지 않고 흐르는 대로가 편해요 | 느긋한 |

3동물 × 4형용사 = 12조합. 형용사 매핑은 문자열 상수 테이블로 구현하며 LLM을 쓰지 않는다. 녹화 시 원하는 조합을 확정적으로 만들어낼 수 있어야 한다.

**닉네임 변경 규칙**

- 기본 닉네임은 진단 결과 화면에서 즉시 보여주고, 그 화면과 이후 프로필 화면에서 자유롭게 변경할 수 있다
- 닉네임은 자유 텍스트 2~12자. 유니크 제약을 두지 않는다(중복 검사 UI를 만들지 않기 위함)
- **종족(개과·고양잇과·곰과)은 변경할 수 없다.** 커뮤니티 글·댓글·프로필에는 닉네임 옆에 종족 배지를 항상 함께 노출한다. 소속감은 이 배지가 담당한다
- 종족과 컬러는 `typeCode`에서 파생되며, 재진단으로 유형이 바뀔 때만 변경된다

컬러는 과 단위로만 고정한다. 형용사까지 컬러를 나누면 치장 아이템 색상이 3종에서 12종으로 불어난다.

---

## 3. 유형 진단 (아키네이터형 대화 진단)

### 판정 책임의 분리

판정은 100% 코드가 한다. LLM은 유형을 말하지 않는다. 이것이 할루시네이션을 구조적으로 0으로 만드는 핵심 설계다.

- **코드가 하는 일**: 선택지를 boolean 지표 14개로 환원하고(`lib/diagnosis/indicators.ts`), 규칙으로 유형을 확정한다(`lib/diagnosis/classify.ts`).
  - 축은 3개: `alone`(가족과 살지 않음), `health`(건강·정서), `econ`(경제)
  - 대분류 규칙:
    1. 가족과 동거하면 → `FAMILY_LIVING`
    2. `alone` + 건강·정서 지표 우세 → `HEALTH_EMOTION`
    3. `alone` + 경제 지표 우세 → `INDEPENDENT_LOW_INCOME`
  - 우울 지표는 선택지가 직접 켜지 않는다. Q3·Q4의 `phq` 값 합이 3 이상일 때 파생된다(PHQ-2 근사).
  - 세부유형 8개(`SubTypeCode`)는 별도로 판정하지만 **화면·API에 절대 노출하지 않는다**. 낙인 위험이 있어 통계·미션 배정용으로만 쓴다.
- **LLM이 하는 일 1가지뿐**: 결과 화면의 판정 근거 3줄 생성(`lib/diagnosis/reason.ts`). 유형은 이미 확정된 뒤라 설명만 한다.
  - 문장 다듬기와 자유 입력 → 선택지 변환은 컷됐다(12절). 진단 흐름에는 LLM 왕복이 없다.

### 문항과 UX

- 문항은 13개(`Q1`~`Q13`)이고 진행률 바를 노출한다. 진행률이 보이지 않으면 이탈이 늘어난다.
- **무손실 조기 종료**: 남은 문항의 모든 조합을 넣어봐도 유형이 하나로 정해지면 그 자리에서 끝낸다(`lib/diagnosis/adaptive.ts`). 결과가 바뀔 수 있는 문항은 절대 건너뛰지 않는다. 실측 평균 9.7문항, 최대 13문항.
- `Q11`~`Q13`은 형용사·통계용이라 판정과 무관하므로 조기 종료 대상에서 빼고 항상 묻는다.
- 모든 문항은 선택지 버튼 3~4개다. 자유 텍스트 입력은 없고, 따라서 진단 중 LLM 지연이 0이다.

### 결과 화면

확정된 유형의 종족·컬러·기본 닉네임(예: "조용한 고양이")을 보여주고, 판정 근거 3줄을 LLM이 생성한다(줄당 60자, 진단·낙인 어휘 금지 목록으로 필터). 근거는 `DiagnosisSession.reasonText`에 캐시한다. 생성이 실패하면 카드만 사라지고 판정은 그대로다. 이 화면에서 닉네임을 바로 수정할 수 있다.

### 재진단

**현재 잠겨 있다**(`lib/diagnosis/flags.ts`의 `REDIAGNOSIS_ENABLED = false`, 2026-08-22 결정). 코드는 남아 있고 진입점만 막았다. 차단은 `POST /api/diagnosis/complete`의 서버 가드 한 곳이고, `/diagnosis`는 이미 진단한 사람을 결과 화면으로 보낸다.

잠근 이유: 재진단으로 종족이 바뀌면 별조각 2500으로 산 옛 종족 펫 스킨이 상점·목록 어디에도 보이지 않은 채 `UserPetSkin`에만 남는다. 환불이냐 유지냐 정책이 없어서 열어 두면 사용자가 조용히 손해를 본다.

플래그를 켜면 이렇게 동작한다. 유형이 바뀌면 `User.typeCode`와 종족·컬러·기본 펫이 갱신되고, 레벨·경험치·진화 단계·보유 재화·보유 아이템은 그대로 유지한다. 활성 펫 스킨은 종족이 바뀔 때만 기본 스킨으로 옮긴다. 기존에 쓴 커뮤니티 글은 작성 당시 갤러리에 그대로 남는다(`Post.galleryType`이 글마다 저장되어 있다).

### 정확도 근거

실사용자가 없어 통계를 낼 수 없으므로 `scripts/check-diagnosis.ts`로 고정한다(`npm run check:diagnosis`). 시나리오 20개, 유형이 갈리는 경계쌍 3개, 이상 입력 4개(빈 답·중복·모르는 코드 등), 근거 3줄 형식 검사 5개. 조기 종료가 결과를 바꾸지 않는지도 여기서 검증한다. 이 스크립트가 발표에서 제시할 정확도 근거다.

---

## 4. 미션

### 구성

| 종류 | 개수 | 초기화 |
|---|---|---|
| 공용 일일 미션 | 5개 | 매일 초기화 (일일 퀘스트) |
| 유형별 단계 미션 | 유형당 **100단계 × 3개 = 300슬롯** (고유 미션 123개) | 초기화 없음 |

**100단계까지 있다 (2026-08-22 확장. 그전에는 3단계 12개였다).** 3단계로는 "방에서 나오는 것"까지만 다루고 그 뒤가 없었다. 100단계를 끝내면 **정기적으로 나가고, 사람과 약속을 잡고, 배우거나 일을 알아보는 상태**가 된다 — 그것이 이 커리큘럼의 종착점이다.

난이도는 고립은둔 회복 순서를 따라 10구간으로 오른다. 구간 이름은 화면에 그대로 뜬다(`lib/missions/bands.ts`의 `BAND_LABELS`). **유형 이름은 어디에도 넣지 않는다** — 낙인 위험이다.

| 구간 | 단계 | 이름 | 내용 |
|---|---|---|---|
| 1 | 1~10 | 방 안에서 | 물 마시기, 커튼 열기, 기지개 |
| 2 | 11~20 | 집 안 생활 | 환기, 설거지 하나, 세수 |
| 3 | 21~30 | 문 앞까지 | 현관 나서기, 복도, 계단 |
| 4 | 31~40 | 동네 한 바퀴 | 해 있을 때 나가기, 걷기 |
| 5 | 41~50 | 가게와 시설 | 마트, 도서관, 상담 창구 알아보기 |
| 6 | 51~60 | 조금 더 멀리 | 지하철, 처음 가는 길 |
| 7 | 61~70 | 한마디 건네기 | 인사, 계산대에서 한마디 |
| 8 | 71~80 | 대화와 모임 | 짧은 대화, 사람 있는 자리 |
| 9 | 81~90 | 관계 이어가기 | 정기 활동, 먼저 말 걸기 |
| 10 | 91~100 | 사회로 한 걸음 | 자기 소개, 약속 잡기, 다음 목표 |

**습관이 되도록 이전 미션을 다시 넣는다.** 300슬롯을 고유 미션 300개로 채우면 전부 단발성이 된다. 대신 고유 미션 **123개**를 평균 **2.44회** 재등장시킨다(최소 1회, 최대 7회). 기본 생활 미션(물 마시기·기지개·이불 정리)이 가장 자주 돌아오고, 후반 미션은 1~2회다. 재등장은 무작위가 아니라 커리큘럼에 고정돼 있어(`prisma/seed/curriculum.ts`) 같은 유형이면 누구나 같은 순서를 본다.

사진 업로드 미션은 **유형당 38~41슬롯**이고 3구간(문 앞까지)부터 붙는다. 방 안 미션에는 넣지 않는다 — 찍을 것이 없다.

수치를 바꾸면 `npm run check:curriculum`이 슬롯 수·재등장 분포·사진 슬롯을 다시 센다.

### 단계 보상

`lib/missions/bands.ts`의 `rewardForStage(stage)`가 유일한 출처다. 구간이 올라갈수록 오른다.

- 씨앗 `18 + 구간 × 4` → 22~58
- 별조각 `구간 - 2` → 0~8. **3구간부터 붙는다** — 방 안 미션에 별조각을 주면 나가지 않는 쪽이 이득이 된다
- 친밀도 0. 미션에서도 주면 커뮤니티·채팅 지급과 겹쳐 하루 상한(`lib/reward.ts` `capAffinity`)을 미션만으로 채운다

100단계 전체 수입은 씨앗 12,000 / 별조각 1,080이다. 변형 스킨이 2,500이므로 별조각은 끝까지 아껴 쓰는 자원으로 남는다.

### 단계 잠금과 해제

1단계는 처음부터 열려 있다. **해당 단계 3개 중 2개를 완료하면 다음 단계가 해제된다.** 3개 전부를 요구하면 오늘 도저히 못 하는 미션 하나가 100단계 전체를 막고, 타겟 특성상 그 지점에서 이탈한다. 사진 미션이 있는 단계에서 나갈 수 없는 날이 그 경우다.

### 초기화 방식

스케줄러를 쓰지 않는다. `User.lastMissionResetAt`을 접속 시점에 비교해서 자정이 지났으면 즉시 초기화한다. 구현이 5줄이고, 서버 재시작이나 Lambda VPC 설정과 무관하다. 심사에서도 "스케줄러 없이 접속 시점 계산으로 설계했다"가 정당한 답변이 된다.

단계 미션은 초기화하지 않는다 — 300슬롯을 한 번씩 지나가는 것이 전체 진행이다.

일일 미션 완료 기록은 `UserMission.resetKey`에 날짜 문자열(`2026-08-20`)을 넣고, 단계 미션은 `STAGE` 고정 문자열을 넣는다. `(userId, missionId, resetKey)` 유니크 제약으로 중복 완료를 DB 레벨에서 막는다.

### 사진 미션 처리

S3 presigned URL로 브라우저가 S3에 직접 업로드하고, 업로드 완료 후 Bedrock Claude Sonnet(비전)으로 미션 달성 여부를 판정한다.

- **판정 방식**: 미션 설명(`Mission.description`)을 프롬프트로 넘겨 LLM이 맥락 판단한다. tool use로 `{ passed: boolean, reason: string }` 형태를 강제한다.
- **실패 처리**: 판정 실패 시 실패 사유(`reason`)를 사용자에게 보여주고 사진을 다시 찍을 수 있도록 안내한다. 미션은 달성되지 않은 상태로 유지한다.
- **흐름**: 업로드 → `/api/upload/verify` 호출 → Bedrock 판정 → 통과 시 미션 달성 처리

### 달성률과 streak

- 일간·주간 미션 달성률을 실시간 표시
- 연속 달성일(streak)을 숫자와 아이콘으로 표시
- streak 판정은 `User.lastStreakDate`와 오늘 날짜 비교

---

## 5. 키우기 시스템

### 펫 성장

- 씨앗 1개 = 경험치 10 (2026-08-19 변경. 이전 값은 1이었다)
- 다음 레벨까지 필요한 경험치 = `현재 레벨 × 100` (1→2는 100, 2→3은 200)
- 위 두 값을 합치면 1→2 레벨업에 씨앗 10개다. 누적 씨앗은 `5 × N × (N-1)` 꼴이라 15레벨이 1,050개, 25레벨이 3,000개다
- 진화는 레벨 기준. **5레벨에서 2단, 15레벨에서 3단, 25레벨에서 4단**으로 진화한다
- **4단 진화다 (2026-08-21 변경. 이전은 3단이었다).** S3에 종당 4장(`-1`~`-4`)이 올라와 있고 4단 진화가 계획된 것임을 E가 확인해 확정했다. 4단 레벨 25는 누적 3,000 씨앗 = 수급 약 109/일 기준 **약 27일**이며, 스킨 2500 별조각(39일)·배경 3600 친밀도(36일)와 결을 맞춘 값이다
- **단계 이름은 알 · 아기 · 청소년 · 성체다.** 이전 3단 구성의 이름(아기·청년·전설)은 쓰지 않는다. 최종 단계는 "전설"이 아니라 "성체"다
- 임계값의 유일한 출처는 `lib/types.ts`의 `EVOLUTION_LEVEL`이고 `npm run check:pet`이 못 박는다. 단계 이름은 `app/pet/_components/PetView.tsx`의 `STAGE_NAME`이 갖는다
- 진화 시 풀스크린 연출 2초
- 이미지는 여우·고양이·곰 × 기본·변종 × 4단 = **24장**이며 S3에 전부 올라와 있다(2026-08-20 실측). `imageKeyBase`는 6종 전부 S3 실제 키와 일치한다 — 북극곰만 `pets/bear-polar`로 어긋나 있던 것을 `pets/bear-arctic`으로 맞췄다(2026-08-21, 시드 + 실 DB)
- 애니메이션은 CSS transform 수준으로만

### 재화 3종

| 재화 | 획득 경로 | 사용처 |
|---|---|---|
| 씨앗 | 일일 미션, 단계 미션, 방치형 자동 획득 | 펫 경험치 투입 |
| 별조각 | **일일 미션 전체 완료 60**, streak 7일 달성, 단계 미션 클리어, 출석 4·7일차 | **종족 외형 스킨 구매** (2026-08-20 팀 확정) |
| 친밀도 | AI 챗봇 대화, 커뮤니티 활동(글 작성·댓글) | **치장 아이템 구매** (2026-08-20 팀 확정) |

**화폐는 상점별로 전용이다 (2026-08-20 팀 확정).** 스킨은 별조각만, 치장은 친밀도만 받는다. 한 상점이 두 화폐를 받으면 어느 재화를 모아야 하는지가 흐려지고, `calculateReward()`의 배율 대상도 재화별로 갈라져 검증이 어려워진다.

### 수급량 확정값 (2026-08-20 팀 확정)

| 재화 | 확정 수급 | 하루 합계 | 소모처 | 전부 모으는 데 |
|---|---|---|---|---|
| 별조각 | **일일 미션 전체 완료 60** + 출석 4일차 5·7일차 20(7일 주기 25) + 단계 3 미션 5 × 4(1회성) | 약 **63.6** | 스킨 2500 | 약 **39일** |
| 친밀도 | **글 작성 20** / 댓글 5 / 챗봇 1턴 5, **하루 누계 상한 100** | 최대 **100** | 배경 6종 × 600 = 3600 | 배경 하나 6일, 전부 **36일** |
| 씨앗 | 일일 미션 60 + 출석 평균 25 + 방치형 24(12시간분) | 약 **109** | 4단 진화까지 누적 3,000 | 약 **27일** |

두 상점의 속도를 비슷하게(36일 · 39일) 맞췄다. **8/26 녹화까지 실제로 모을 수 있는 기간이 아니므로 데모 계정에 별조각·친밀도·보유 아이템을 시드로 넣는다**(`업무분담.md` 5장). 실사용자가 없는 데모 프로젝트에서 재화 곡선은 시연 화면이 아니라 "설계가 성립하는가"를 보이는 값이다.

### 데모 계정 재화 시드값 (2026-08-21 확정)

위 표의 소모처 값에서 그대로 파생시킨다. 임의의 큰 숫자를 넣지 않는다 — 재화가 남으면 화면의 진행률이 "설계된 속도"를 보여주지 못한다.

| 재화 | 시드값 | 근거 |
|---|---|---|
| 씨앗 | **3,000** | 4단 진화(Lv.25) 누적치. 녹화 중 2단·3단·4단 연출을 순서대로 다 보여줄 수 있는 최소값이다 |
| 별조각 | **2,500** | 스킨 1종 정가. 딱 맞게 둬서 구매 후 잔액 0이 되는 것까지 화면에 남는다 |
| 친밀도 | **3,600** | 배경 6종 합계. 도감 진행률(`3/6`)과 슬롯 교체를 둘 다 찍을 수 있다 |

레벨·경험치는 심지 않는다. 진화 연출은 녹화 중에 씨앗을 먹여서 만들어야 하고, 레벨을 미리 올려 두면 그 장면이 사라진다.

유일한 출처는 `scripts/seed-demo-currency.ts`다. **공유 개발 DB에 쓰므로 실행은 승인을 받고 한다.** 이 스크립트는 `calculateReward()`를 경유하지 않는다 — 저 함수는 앱 안에서 재화를 *획득*하는 경로에 배율을 얹는 것이고(6절), 테스트 데이터 심기에 배율을 태우면 3,000이 3,450이 되어 검증값이 흐려진다.

**별조각 60은 아직 구현되지 않았다.** `lib/missions/completion.ts:128`에 TODO로 남아 있다(B 담당). 그게 들어가기 전에는 별조각 수급이 출석 3.6/일뿐이고 스킨 2500은 약 700일 — 상점이 사실상 잠긴 상태다.

씨앗은 4단 진화(누적 3,000) 이후 소모처가 없다. 스킨·치장이 둘 다 씨앗을 받지 않으므로 남는 것이 정상이며, 씨앗 소모처를 새로 만들지 않는다.

**4단으로 늘리면서 세 상점의 속도가 27일 · 36일 · 39일로 모였다.** 3단이던 때 씨앗만 9일로 혼자 빨라 펫이 먼저 끝나고 남는 재화가 되던 문제가 함께 풀린다.

친밀도는 **하루 최대 100까지만** 지급한다. 챗봇 1턴 5, 글 작성 20, 댓글 5로 하고 누계가 100에 도달하면 이후 지급은 0이다. `User.affinityToday`와 `affinityTodayDate`를 접속 시점에 비교해 리셋한다(미션 초기화와 같은 패턴).

오프라인 모임 기능을 추후 구현하면 모임 참석 시 친밀도를 대량 지급한다. 이번 범위에는 포함하지 않는다.

### 종족 외형 스킨 (별조각 전용)

같은 종족의 다른 외형이다. **능력치는 바뀌지 않고 외형만 바뀐다** — `effectType`은 전부 `NONE`이고, 기본 외형과 똑같이 4단 진화한다(`PetSkin.stageCount = 4`).

| 스킨 | 종족 | 기본 외형 | 가격 |
|---|---|---|---|
| 북극여우 | 개과 | 여우 | 별조각 2500 |
| 북극고양이 | 고양잇과 | 고양이 | 별조각 2500 |
| 북극곰 | 곰과 | 곰 | 별조각 2500 |

세 스킨의 값은 같다. 종족마다 다르면 가격만 보고 어느 유형으로 진단됐는지 알 수 있게 되고, 그건 2절의 낙인 방지와 어긋난다.

**자기 종족의 스킨만 구매할 수 있다.** 남의 종족 스킨을 사면 진단 결과와 어긋난 동물이 화면에 뜬다. 서버에서 `skin.typeCode !== user.typeCode`를 막고(화면 필터만으로는 id를 직접 던지면 뚫린다), 거절 문구에 유형 이름을 쓰지 않는다(낙인. 2절).

기본 외형(여우·고양이·곰)은 진단으로 지급되며 `priceShards = null`로 구매 불가를 표현한다.

### 치장 아이템 (친밀도 전용)

**배경 6종이다 (2026-08-20 변경).** 이름은 `배경1`~`배경6`이고 **종족 구분이 없어 누구나 쓸 수 있다.** 슬롯당 1개만 착용하므로, 슬롯이 하나인 지금은 "배경 하나 고르기"가 된다.

| 아이템 | 슬롯 | 등급 | 가격 |
|---|---|---|---|
| 배경1 ~ 배경6 | BACKGROUND | COMMON | 각 친밀도 600 |

**착용한 배경은 펫 화면의 방 배경으로 표시된다 (2026-08-21 확정).** 아무것도 착용하지 않았으면 종족색을 따르는 기본 방이 나온다(`app/pet/_components/PetRoom.tsx`). 카펫·바닥·창은 **배경에 속하는 요소**이므로 배경이 갈리면 함께 갈린다 — 별도 레이어로 두지 않는다. 배경 그림은 **불투명 전체 그림**이어야 하고, 펫은 배경과 무관하게 방 중앙 하단에 선다.

이전 구성(12종 = 모자·목도리·배경 각 4개)은 컷했다. 이미지를 12장 만들 시간이 없고, 슬롯이 하나면 "슬롯당 1개"가 곧 "배경 하나 고르기"가 되어 화면도 단순해진다.

6종 합계는 **3600 친밀도**다. 일 상한 100 기준 배경 하나에 6일, 전부 모으는 데 36일이다. 녹화용 수집 진행률 화면은 데모 계정 시드로 만든다(위 "수급량 확정값").

`Slot` enum의 `HAT`·`SCARF`는 스키마에 그대로 둔다. 시드에서 쓰지 않으면 행이 생기지 않아 마이그레이션이 필요 없고, `schema.prisma`는 전원 합의 파일이다.

`CosmeticItem.rarity`는 추첨 확률이 아니라 **가격 기준**이다(가챠 컷 이후 등급의 용도를 가격으로 바꿨다).

| 등급 | 가격 |
|---|---|
| COMMON | 친밀도 600 |
| RARE | 친밀도 1000 |
| EPIC | 친밀도 1800 |
| LEGENDARY | 친밀도 2800 |

COMMON만 600으로 올리면 RARE가 COMMON보다 싸지므로 나머지 등급도 함께 10배로 올렸다. 지금 6종이 전부 COMMON이라 RARE 이상은 쓰이지 않지만, 순서가 뒤집힌 표를 남기면 등급만 바꾼 사람이 가격이 내려간 것을 모른다. `npm run check:pet`이 등급 오름차순을 단정한다.

**가격은 등급에서 파생시키고 아이템마다 손으로 적지 않는다.** 시드(`prisma/seed/items.ts`의 `PRICE_BY_RARITY`)가 유일한 출처이며 `upsert`가 매번 `priceAffinity`를 이 표로 덮어쓴다. 아이템을 추가할 때는 등급만 정하면 되고, 등급을 새로 만들려면 `Rarity` enum 변경이 필요해 전원 합의 사항이다.

배경 6종은 **등급을 가르지 않았다.** 6종이 서로 대체재라 값 차이가 유저에게 주는 정보가 없다. 다시 가르려면 시드의 `rarity`만 바꾼다. 구성은 `npm run check:pet`이 못 박아 둔다(6종·전부 배경·전부 COMMON·COMMON 600·합계 3600·등급 오름차순·변종 스킨 2500).

**시드가 목록에서 빠진 치장을 지운다.** `upsert`는 `name`이 유니크 키라 옛 행을 남기므로, 12종에서 6종으로 줄일 때 `pruneCosmetics()`가 필요해졌다. 보유자(`UserCosmetic`)가 있는 행은 지우지 않고 경고만 남긴다 — 시드는 유저 데이터를 지우지 않는다.

**가챠 기능 컷 (2026-08-19, 팀 결정)** — 확률형 뽑기·천장(`heroPity`/`legendPity`)·`GachaPull` 모델을 전부 제외했다. 가챠가 유일한 획득 경로였던 문제는 2026-08-20에 위 두 상점으로 닫았다(스킨=별조각, 치장=친밀도).

### 출석 보상 캘린더

7일 그리드이며 7일 주기로 반복한다. `dayIndex = (User.attendanceTotal - 1) % 7 + 1`로 계산한다. 7일차 보상을 받은 다음 접속일은 다시 1일차다. 매일 보상을 지급하고 7일차에 별조각을 큰 보상으로 준다. streak과 별개로 접속 자체에 대한 보상이다.

### 방치형 자동 획득

`User.lastIdleClaimAt`과 현재 시각의 차이로 씨앗을 누적 계산하고, 접속 시 수령한다. 상한을 두어(12시간분) 무한 누적을 막는다.

**시간당 2개** (2026-08-19 C 결정). 상한 12시간분이므로 한 번에 최대 24개, 하루 최대 48개다. 일일 미션 5개가 60개를 주므로 방치형이 미션 경로를 넘지 않는다 — 넘으면 "미션을 하러 오는 앱"이 아니라 "켜 두는 앱"이 된다. 값은 `lib/pet.ts`의 `IDLE_SEEDS_PER_HOUR` 한 곳에 있고 `npm run check:pet`이 못 박아 둔다.

- 첫 접속(`lastIdleClaimAt`이 `null`)은 소급 지급하지 않고 기준 시각만 심는다. 가입 즉시 24개를 주면 방치형이 아니라 가입 보너스가 된다
- 1개(30분)에 못 미치는 자투리 시간은 다음 수령으로 넘긴다. 기준 시각을 수령 시각으로 밀면 자주 들여다보는 유저가 영구히 손해를 본다
- 상한을 넘긴 초과분은 버린다. 이때만 기준 시각이 수령 시각으로 이동한다
- 수령은 유저가 버튼을 눌렀을 때만 한다(`POST /api/pet/idle`). 화면을 열기만 해도 DB 쓰기가 나가면 안 되고, 받는 순간을 보여 주는 것이 보상으로 읽힌다
- 획득이므로 `calculateReward()`를 통과한다(6절). 씨앗 +15% 캐릭터는 24개가 27개가 된다
- 계산은 `lib/pet.ts`의 `idleAccrual(lastClaimAt, now)` 순수 함수에 있다. 이중 지급은 `lastIdleClaimAt`을 `where`에 넣은 조건부 UPDATE로 막는다

### 배고픔 게이지 (2026-08-21 추가)

펫 화면에 배고픔 게이지를 둔다. **표시 전용이며 재화·경험치·진화 단계에 영향을 주지 않는다.**

- `User.lastFedAt`(마지막으로 씨앗을 먹인 시각)에서 경과한 시간으로 계산한다. 100 = 배부름, 0 = 비었음
- **24시간에 걸쳐 100 → 0으로 선형 감쇠한다.** 하루 한 번 들여다보면 유지되는 값이다
- 배고픔 값 자체는 저장하지 않는다. 저장하면 화면을 열 때마다 감쇠분을 써야 해서 조회가 쓰기가 된다(방치형 씨앗을 페이지 로드 때 지급하지 않는 것과 같은 이유)
- **투입한 씨앗 개수는 배고픔에 영향을 주지 않는다.** 1개를 먹여도 100이 된다. "얼마나 먹였나"는 경험치가 이미 표현하고, 배고픔은 "얼마나 들여다봤나"만 본다
- **0이 되어도 잃는 것이 없다.** 페널티를 붙이면 아래 "제외한 것"의 랭킹 배제와 같은 이유로 어긋난다 — 대상 이용자에게 벌점형 압박을 주지 않는다. 페널티를 넣으려면 이 절을 먼저 고친다
- 한 번도 먹이지 않은 유저(`lastFedAt`이 `null`)는 `createdAt`을 기준으로 쓴다. 신규 유저에게 0을 보여 주면 시작부터 굶긴 것처럼 보인다
- 계산은 `lib/pet.ts`의 `hungerFor(since, now)` 순수 함수에 있고 `npm run check:pet`이 못 박아 둔다

### 제외한 것

- 랭킹·경쟁 지표: 타겟이 고립은둔청년이므로 순위 비교는 위축과 이탈을 유발한다. 의도적으로 배제하며, 발표에서 이 배제를 설명 카드로 사용한다. 필요하면 "커뮤니티 전체 미션 달성 합계" 같은 협력형 지표로 대체한다.
- 별도 도감 화면: 치장 아이템 목록 화면에 `3/6` 형태의 진행률과 미획득 실루엣을 표시해 겸용한다. 분모는 하드코딩하지 않고 `CosmeticItem` 행 수에서 나온다.
- 미니게임, 펫 꾸미기 에디터

---

## 6. 공유 함수 2개 (설계 강제 사항)

### `lib/auth.ts` — 소유자 E

모든 API Route Handler의 첫 줄에서 호출한다. Cognito 토큰을 검증하고 `cognitoSub`으로 `User`를 조회한다. 처음 로그인한 사용자는 이 안에서 생성(upsert)한다.

```ts
// lib/auth.ts — 소유자: E (인프라·인증 담당)
export async function getCurrentUser(): Promise<User>
// 미인증이면 throw. 호출부는 401 응답으로 변환
```

5명 전원이 이 함수에 의존한다. **8/15까지 완성되어야 하며, 그 전에는 개발용 고정 유저를 반환하는 스텁으로 대체한다.**

### `lib/reward.ts` — 소유자 C

캐릭터 고유 효과는 이 한 함수에서만 적용한다. 미션 보상, 방치형 수령, 출석 보상, 챗봇·커뮤니티 친밀도 지급이 전부 이 함수를 통과해야 한다.

```ts
// lib/reward.ts — 소유자: C (펫 담당)
type RewardInput = { seeds?: number; starShards?: number; affinity?: number }

export function calculateReward(skin: PetSkin | null, base: RewardInput): RewardInput
// skin의 effectType/effectPct를 읽어 해당 재화에만 배율 적용
```

`User`가 아니라 `PetSkin | null`을 받는다. `User`만 받으면 함수 안에서 활성 스킨을 다시 조회해야 하고 그러면 async가 되어 호출부 4명의 코드가 전부 바뀐다. 호출부는 유저를 가져올 때 `include: { activePetSkin: true }`를 붙이고 `user.activePetSkin`을 넘긴다.

각 기능이 자기 자리에서 `user.seeds += 10` 같은 코드를 직접 쓰면 5인 병렬 작업에서 반드시 어긋난다. 재화 증감은 예외 없이 이 함수를 거친다.

---

## 7. AI 상담사 (일상 대화 챗봇)

- 유형별 페르소나를 가진 공감 챗봇. **해결책을 제시하지 않는다.**
- Bedrock Claude Sonnet, 스트리밍 응답
- 대화는 `ChatMessage`에 저장
- 대화 1턴마다 친밀도 5를 지급하되 5절의 일일 상한 100을 공유한다
- 실사용자가 없으므로 위기 신호 대응 프로토콜은 이번 범위에 넣지 않는다. 실사용자 오픈 시 필수 항목이며 발표의 "향후 계획"에 명시한다

---

## 8. 커뮤니티

디시인사이드 구조를 차용한다.

- 유형별 갤러리 3개(과 단위, 컬러로 구분)
- 목록 → 상세 → 글쓰기 → 댓글 → 좋아요 → 본인 글·댓글 삭제
- 작성자 표기는 `닉네임 + 종족 배지`. 예: "조용한 고양이 · 고양잇과"
- 글쓰기 창 진입 시 LLM이 사용자 성향에 맞는 작성 주제·초안을 3가지 이상 추천
- 일일 미션에 "커뮤니티 글 작성"을 포함
- 글 작성 시 친밀도 20, 댓글 작성 시 5 지급 (일일 상한 100 공유)

제외: 신고, 차단, 페이지네이션(최근 20개 고정). 이미지 업로드는 사진 미션용 S3 인프라가 이미 있으므로 여력이 남으면 확장한다.

---

## 9. 희망 문구 배너

커뮤니티 메인 배너에 노출한다. 문구는 상수 파일에 3~5개 하드코딩하고 **주 1회 단위로만** 교체한다. 자주 바뀌면 사용자가 피로해진다.

---

## 10. 아키텍처와 AWS 구성

| 서비스 | 용도 | 심사 설명 |
|---|---|---|
| Amplify Hosting | Next.js 빌드·배포·HTTPS·CDN. `main` push 시 자동 배포 | 서버리스 SSR + CloudFront, 유휴 비용 0 |
| RDS Postgres `db.t4g.micro` | 데이터베이스. Prisma ORM | 자동 백업 7일 보존으로 데이터 소실 방지 |
| Cognito | 이메일 + 비밀번호 인증, Google 로그인(Hosted UI) | 인증·토큰 관리를 관리형 서비스에 위임 |
| Bedrock (us-east-1, Claude Sonnet 단일) | 진단 질문 다듬기, enum 변환, 챗봇, 주제 추천 | 모델 분기 없음 = 버그 표면 최소 |
| CloudWatch + SNS | CPU·에러율 대시보드, 알람 1개 | 운영 모니터링 |
| S3 + CloudFront | 사진 미션 업로드, 펫·치장 이미지 | presigned URL 직업로드, 정적 자원 분리 |

- 서버 로직은 Next.js Route Handler로 구현한다. 별도 API Gateway·Lambda 레포를 만들지 않는다.
- Bedrock 모델 접근은 확인 완료(Playground 응답 정상). 정확한 모델 ID는 콘솔에서 복사해 `.env`로 관리한다.
- 이메일+비밀번호와 Google 로그인을 함께 지원한다(결정 변경, `docs/STATUS.md` 참고). 이메일 인증 코드는 끄고 `AdminCreateUser`로 즉시 가입시킨다(SES 검증 대기 회피).
- 이메일 로그인은 `app/api/auth/{signup,login}`이 Cognito Admin API를 직접 호출한다. Google은 `app/api/auth/google`이 Cognito Hosted UI(Domain: `welli-auth-185236887369`)로 리다이렉트하고 `app/api/auth/callback`이 code를 토큰으로 교환한다. 로그인 성공 시 `access_token`을 httpOnly 쿠키에 담아 `lib/auth.ts`가 그 쿠키만 읽는다(Authorization 헤더는 문서 내비게이션에 안 붙어서 쓰지 않는다).

### 채택하지 않은 것

EC2, Docker, Elastic Beanstalk, App Runner, EventBridge, Lambda, WAF, Secrets Manager. 선정 기준은 이해 가능성·버그 발생률·발표 시점 안정성이었다. 비밀값은 Amplify 환경변수로 관리한다.

### 보안 설명 카드

Cognito 기반 인증, Amplify 관리형 HTTPS 강제, Prisma parameterized query로 SQL Injection 차단, IAM 최소 권한, S3 버킷 비공개 + presigned URL.

> **RDS 퍼블릭 액세스는 아직 열려 있다 (2026-08-22 실측).** 전에 이 줄에 "RDS는 퍼블릭 액세스 차단"이라고
> 적혀 있었는데 사실이 아니다 — 인스턴스가 Publicly Accessible이고 보안 그룹(`sg-0e61ffef88ff3726e`)의
> 5432가 `0.0.0.0/0`이다. 지금 방어는 강한 마스터 비밀번호 하나뿐이다. **발표 전에 닫거나, 못 닫으면
> 이 카드에서 그 문장을 빼고 "개발 편의를 위해 열어 뒀고 운영 전환 시 VPC 내부로 제한"이라고 말한다.**
> 없는 통제를 있다고 말하면 심사에서 그 한 줄로 신뢰를 잃는다.

---

## 11. 데이터 모델

**스키마 본문은 `prisma/schema.prisma`가 유일한 원본이다.** 이 문서에 복사해 두면 두 곳이 갈라지므로 여기에는 규칙만 적는다. 변경 절차는 `CLAUDE.md` 1절을 따른다.

스키마를 읽을 때 알아야 할 설계 의도:

| 항목 | 의도 |
|---|---|
| `User.email` / `passwordHash` (2026-08-21 추가) | 자체 DB 계정용. Cognito로 가입한 계정(Google 로그인)은 둘 다 `null`이다. `passwordHash`는 `lib/password.ts`의 scrypt 형식이며 평문은 저장하지 않는다 |
| `User.cognitoSub` | NOT NULL·유니크다. 자체 계정에는 Cognito sub가 없으므로 `local:<uuid>`를 넣는다 — Cognito sub는 UUID라 접두사가 있으면 절대 겹치지 않는다 |
| `UserMission.@@unique([userId, missionId, resetKey])` | 중복 완료를 DB가 막는다. 애플리케이션에서 중복 체크를 하지 않는다 |
| `User.activePetSkin` relation | `calculateReward()`에 넘길 활성 스킨을 `include` 한 번으로 가져오기 위한 것 |
| `User.affinityToday` / `affinityTodayDate` | 친밀도 일일 상한 100 계산. 접속 시점 비교로 리셋 |
| `User.attendanceTotal` | 출석 `dayIndex`를 7일 주기로 계산하기 위한 누적값 |
| `User.lastFedAt` | 배고픔 게이지를 계산하기 위한 마지막 급여 시각. 배고픔 값 자체는 저장하지 않는다(5절). `null`이면 아직 안 먹인 것이고 화면이 `createdAt`을 대신 쓴다 |
| `PetSkin.stageCount` | 전부 4다(2026-08-21에 3에서 올렸다). 외형 스킨도 기본 외형과 같이 진화하므로 이미지 키에 `-1 -2 -3 -4` suffix가 붙는다 |
| `PetSkin.priceShards` | 별조각 구매가. 기본 외형은 `null`(구매 불가). 스킨은 별조각 전용이다 |
| `CosmeticItem.rarity` | 추첨 확률이 아니라 **가격 기준**이다. 가격은 시드의 `PRICE_BY_RARITY`에서 파생된다 |
| `CosmeticItem.affinityOnly` | 치장은 전부 친밀도 전용이라 항상 `true`다 |
| `Post.galleryType` | 작성 시점 갤러리를 글에 박아둔다. 재진단으로 유형이 바뀌어도 글은 이동하지 않는다 |
| `Post.likeCount` / `commentCount` | 비정규화 컬럼. 목록에서 집계 쿼리를 피하기 위한 것이며 좋아요·댓글 API에서 함께 갱신한다 |

### API 경로 규칙

기능별로 접두사를 나눠 5인이 같은 파일을 건드리지 않게 한다.

```
/api/diagnosis/*    A
/api/missions/*     B
/api/upload/*       B   (presigned URL 발급)
/api/pet/*          C
/api/community/*    D
/api/chat/*         D
/api/auth/*         E
```

### API 응답 형식

```ts
// 성공
{ data: { ... } }

// 실패
{ error: { code: "MISSION_ALREADY_DONE", message: "이미 완료한 미션입니다" } }
```

HTTP 상태 코드는 `200` `400` `401` `404` `500`만 쓴다. `message`는 화면에 그대로 띄울 한국어 문장으로 쓴다.

---

## 12. 이번 범위에서 제외한 기능 (발표 "향후 계획" 슬라이드용)

- 오프라인 모임 주최: 관리자 파티원 모집 글, 정기 모임 자동 갱신, 사용자 건의함. 운영 인력과 대면 안전 책임이 필요해 이번 범위에서 제외. 구현 시 모임 참석에 친밀도를 대량 지급하는 연결 고리가 이미 설계되어 있다
- 실사용자 대상 위기 신호 대응 프로토콜, 개인정보 처리방침·민감정보 별도 동의
- 커뮤니티 이미지 업로드, 신고·차단
- 커뮤니티 협력형 지표(전체 미션 달성 합계)
- 가챠(확률형 뽑기): 2026-08-19 중간 체크포인트에서 팀 결정으로 컷. 개발 리소스 대비 우선순위가 낮다고 판단. 치장의 대체 획득 경로와 별조각 소모처는 2026-08-20에 상점 2개(스킨=별조각, 치장=친밀도)로 확정했다. 5절 참조

---

## 13. 심사 예상 질문 대응

| 질문 | 답변 |
|---|---|
| 비용 최적화를 어떻게 했나 | Amplify 서버리스 SSR로 유휴 비용 0, RDS는 `t4g` ARM 최소 인스턴스, 정적 자원은 S3+CloudFront로 분리, LLM은 단일 모델로 호출 경로를 단순화 |
| 트래픽 관리는 | CloudFront 캐싱 + Amplify 자동 확장. DB 커넥션은 Prisma 커넥션 풀로 제한 |
| 보안은 | Cognito 인증, HTTPS 강제, Prisma parameterized query, IAM 최소 권한, S3 비공개 + presigned URL. **RDS 퍼블릭 액세스는 개발 편의로 열어 뒀고 운영 전환 시 VPC 내부로 제한한다**(위 "보안 설명 카드" 경고 참고 — 차단했다고 말하지 않는다) |
| AI 정확도는 어떻게 담보하나 | 유형 판정은 LLM이 아니라 코드 규칙이 수행. LLM은 결과 화면의 근거 3줄 생성만 담당하고, 그 호출이 실패해도 판정은 바뀌지 않는다. 시나리오 20개 + 경계쌍 3 + 이상 입력 4를 `npm run check:diagnosis`로 고정 |
| 왜 랭킹이 없나 | 고립은둔청년 대상 서비스에서 순위 비교는 위축과 이탈을 유발한다고 판단해 의도적으로 배제. 협력형 지표로 대체할 계획 |
| 24시간 동작 증명 | Amplify 배포 URL 상시 접근 가능, CloudWatch 대시보드로 가동 지표 제시 |
