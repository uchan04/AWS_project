import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: "test@welli.local" } }); if (!user) return console.log("User not found"); const now = new Date(); now.setMinutes(now.getMinutes() + 1); await prisma.petOuting.updateMany({ where: { userId: user.id, claimedAt: null }, data: { returnsAt: now } }); console.log("Updated returnsAt to 1 minute from now for test@welli.local"); } main().finally(() => prisma.$disconnect());