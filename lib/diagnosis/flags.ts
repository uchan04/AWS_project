// 소유자: A. 진단 기능 스위치.
//
// 재진단은 SPEC.md 3절에 있고 코드도 살아 있지만 지금은 잠가 둔다(2026-08-22 팀 결정).
// 이유: 재진단으로 종족이 바뀌면 산 펫 스킨이 상점·목록 어디에도 안 보인 채
// UserPetSkin에만 남는다. 환불이냐 유지냐 정책이 없는 상태에서 열어 두면
// 별조각 2500을 쓴 사용자가 조용히 손해를 본다(docs/dev/pet.md 801행).
//
// 여기를 true로 바꾸면 다시 열린다. 잠금 지점은 세 곳뿐이다:
//   - POST /api/diagnosis/complete  (서버 가드. 이것만이 실제 차단이다)
//   - /diagnosis                    (이미 진단했으면 결과로 보낸다)
//   - 홈·결과·사이드바의 "다시 진단하기" 링크
//
// 클라이언트도 import하므로 서버 전용 모듈을 여기에 넣지 않는다.
export const REDIAGNOSIS_ENABLED = false
