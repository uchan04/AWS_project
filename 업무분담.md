# 업무 분담과 일정

> **⚠️ 이 문서의 미션 수치는 낡았다 (2026-08-23 확인).** 아래 본문에 남은 "미션 41개",
> "유형당 12 × 3", "단계 4개 중 3개 완료", "3단계"는 2026-08-22 커리큘럼 확장 **전**의
> 값이다. 일정·담당 경계를 보려고 이 문서를 읽는 것은 그대로 유효하고, 수치만 아래를 따른다.
>
> | 항목 | 이 문서(낡음) | 실제 |
> |---|---|---|
> | 단계 수 | 3 | **100** (`lib/missions/bands.ts` `TOTAL_STAGES`) |
> | 단계당 미션 | 4 | **3** (`MISSIONS_PER_STAGE`) |
> | 해금 조건 | 4개 중 3개 | **3개 중 2개** (`REQUIRED_PER_STAGE`) |
> | 미션 총량 | 41개 | 고유 **123개**를 유형당 **300슬롯**에 재등장시킨다 |
>
> 정본은 `lib/missions/bands.ts`(상수) · `prisma/seed/curriculum.ts`(콘텐츠) ·
> `SPEC.md` 4절 · `docs/dev/missions.md`다. 수치가 어긋나면 그쪽을 믿는다.

기준 문서: `SPEC.md` (기능·데이터 모델 확정 명세)
기간: 2026-08-14 ~ 2026-08-28

분담 방식은 **기능 수직 분할**이다. 한 사람이 하나의 기능을 화면부터 API, DB까지 세로로 관통한다. 레이어로 나누면(프론트 2 / 백 2) 모든 기능이 두 명을 거쳐야 끝나서 대기가 생기고, 8일 안에 15인일 분량을 소화할 수 없다.

담당자 표기는 A~E다. 실제 이름으로 바꿔 쓰되 역할 경계는 유지한다.

---

## 1. 담당별 범위

### A — 유형 진단 + 미션 콘텐츠

**산출물**
- 진단 6문항 정의(문항 코드, 선택지, axis, weight)
- 판정 함수: 답변 배열 → `{ typeCode, adjective, axisScores }`
- 형용사 매핑 테이블 (6번 문항 → 4형용사)
- Bedrock 호출 2종: 질문 문장 다듬기, 자유 답변 → enum 변환(tool use, 폴백 포함)
- 결과 화면(종족·컬러·근거 3줄) + 기본 닉네임 자동 세팅과 즉시 변경 UI
- 프로필 재진단 (유형만 갱신, 재화·아이템·레벨 유지)
- 시나리오 18개 스냅샷 테스트
- **미션 콘텐츠 41개** (공용 일일 5 + 유형당 12 × 3) — 8/15까지 초안

**API**: `/api/diagnosis/*`

**주의**: 미션 콘텐츠 41개는 8/15까지 끝내야 한다. B가 더미 5개로 선행 개발하지만 8/16 이후에는 실제 콘텐츠가 필요하다. PDF 연구 요약을 근거로 Claude로 초안을 만들고 전원 15분 검수 후 `prisma/seed.ts`에 고정한다.

---

### B — 미션 시스템 + 사진 업로드

**산출물**
- 일일 미션 목록 화면, 단계 미션 목록 화면 (해제 조건: 해당 단계 4개 중 3개 완료)
- 접속 시점 초기화 로직 (`lastMissionResetAt` 비교, 스케줄러 없음)
- 미션 완료 처리 (`UserMission` 유니크 제약으로 중복 방지)
- 일간·주간 달성률, streak 표시
- 출석 보상 7일 캘린더 (`dayIndex = (attendanceTotal - 1) % 7 + 1`, 7일 주기 반복)
- S3 presigned URL 발급 API + 브라우저 직업로드 + 사진 미션 달성 처리

**API**: `/api/missions/*`, `/api/upload/*`

**주의**: 미션 보상 지급은 반드시 `calculateReward()`를 통과시킨다. `user.seeds += n`을 직접 쓰지 않는다.

---

### C — 펫 + 스킨

**컷 (2026-08-19, 팀 결정, 중간 체크포인트)**: 가챠 기능 전체(확률형 뽑기, 천장, 중복 환급)를 범위에서 제외했다. 가챠가 유일한 경로였던 치장 획득과 별조각 소모처는 **2026-08-20에 상점 2개로 닫았다** — 스킨은 별조각 전용, 치장은 친밀도 전용. 가격·수급량도 같은 날 확정해 시드·실 DB에 반영했다 (`docs/dev/pet.md` 참고)

**산출물**
- **`lib/reward.ts` 먼저 만든다** — `calculateReward(skin, base)`. 8/16 오전까지 공유. 4명이 이걸 기다린다
- 펫 화면: 레벨·경험치, 씨앗 투입, 진화 + 풀스크린 연출 2초 (**씨앗 1 = exp 10**, 필요 exp = 레벨 × 100, 5레벨 2단 / 15레벨 3단)
- 방치형 자동 획득 (`lastIdleClaimAt` 시간차, 시간당 2개, 상한 12시간분)
- 치장 **배경 6종**(`배경1`~`배경6`) 구매·착용·해제 (슬롯당 1개), 목록에 수집 진행률 표시. 친밀도 전용이고 가격은 등급에서 파생된다 — 6종 전부 COMMON = 각 **600**(합 3600). 2026-08-20에 12종(모자·목도리·배경)에서 줄였다
- 종족 외형 스킨 3종(북극여우·북극고양이·북극곰) 구매·전환. 각 별조각 **2500**, **자기 종족만 구매 가능**. 능력치 효과는 없고 외형만 바뀐다(3단 진화는 기본 외형과 같다)
- **수급량 확정 (2026-08-20)**: 일일 미션 전체 완료 = 별조각 60 / 글 작성 = 친밀도 20, 일 상한 100. 스킨 39일 · 배경 전부 36일 속도다. **별조각 60은 `lib/missions/completion.ts:128`에 미구현(B)** — 그게 없으면 스킨이 약 700일이다

**API**: `/api/pet/*`

**주의**: `calculateReward()`는 4명이 모두 호출하는 공유 함수다. 시그니처를 바꿀 때는 전원에게 알린다.

---

### D — 커뮤니티 + 챗봇 + 친밀도 지급

**산출물**
- 유형별 갤러리 3개: 목록(최근 20개) → 상세 → 글쓰기 → 댓글 → 좋아요 → 본인 글·댓글 삭제
- 작성자 표기는 `닉네임 + 종족 배지` (예: "조용한 고양이 · 고양잇과")
- 글쓰기 창 진입 시 LLM 주제·초안 3가지 추천
- AI 상담 챗봇: 유형별 페르소나, 스트리밍, 해결책 제시 금지, 대화 저장
- 친밀도 지급 로직 — 챗봇 1턴 5 / 글 20 / 댓글 5, **하루 누계 상한 100 공유**. `affinityToday`·`affinityTodayDate`를 접속 시점에 비교해 리셋

**API**: `/api/community/*`, `/api/chat/*`

**주의**: 챗봇 시스템 프롬프트에 "조언·해결책을 제시하지 말고 공감과 경청에 집중" 지시를 명시한다. 친밀도 지급도 `calculateReward()` 경유.

---

### E — 인프라 + 인증 + 발표 자료

**산출물 (8/14 — 이게 끝나야 나머지 4명이 시작할 수 있다)**
- GitHub 레포 생성 + Next.js 프로젝트 생성 + 첫 push, 전원 clone 확인
- **`lib/auth.ts`의 `getCurrentUser()`** — 5명 전원이 API 첫 줄에서 호출한다. 8/14에는 개발용 고정 유저를 반환하는 스텁이라도 올려서 4명의 착수를 막지 않는다. 실제 Cognito 검증은 8/15까지
- 담당별 브랜치 5개 생성 (`feat/diagnosis` `feat/missions` `feat/pet` `feat/community` `feat/infra`)

**산출물 (8/15)**
- Amplify Hosting: GitHub 연결, `main` push 자동 배포, 빈 화면이라도 라이브
- RDS Postgres `db.t4g.micro`: 퍼블릭 액세스 차단, 자동 백업 7일
- Cognito: 이메일+비밀번호, 이메일 인증 코드 비활성
- Bedrock 연결 확인(us-east-1, Sonnet), 모델 ID를 `.env`에 고정
- S3 버킷 + CloudFront (비공개 버킷, presigned URL 전용)
- CloudWatch 대시보드 + SNS 알람 1개(CPU 70%)
- Prisma 초기 마이그레이션, `seed.ts` 골격

**산출물 (8/16 이후)**
- 로그인·회원가입·로그아웃 화면
- 희망 문구 배너 (상수 3~5개, 주 1회 교체)
- **8/20부터 발표 자료 착수**
- CloudWatch 대시보드·Cost Explorer 화면 캡처 확보

**API**: `/api/auth/*`

---

## 2. 공유 계약 (건드릴 때 전원 합의 필요)

| 파일 | 소유자 | 규칙 |
|---|---|---|
| `prisma/schema.prisma` | 전원 | 8/15까지 확정. 이후 변경은 전원 합의 + `SPEC.md` 동시 갱신 |
| `lib/auth.ts` | E | `getCurrentUser()`. 모든 API의 첫 줄 |
| `lib/reward.ts` | C | `calculateReward()`. 재화 증감의 유일한 경로 |
| `lib/types.ts` (enum·상수) | A | TypeCode, Adjective, 표시명 매핑 |
| `prisma/seed/missions.ts` | A | 미션 41개 |
| `prisma/seed/items.ts` | C | 스킨 6종(기본 3 + 변종 3) + 치장 배경 6종. 가격의 유일한 출처 |
| `prisma/seed.ts` | E | 위 두 파일을 호출하는 엔트리. 3줄 |
| `.env.example` | E | 키 추가 시 Amplify 환경변수에도 함께 등록 |

시드는 파일을 나눈다. 한 파일을 A와 C가 나눠 쓰면 충돌이 확정이다.

**이 파일들은 기능 브랜치에서 고치지 않는다.** 소유자가 `main`에 직접 커밋하고 전원에게 알린다. 공유 파일이 브랜치에 들어가면 5개 브랜치가 서로 충돌한다.

기능별 폴더를 분리한다: `app/diagnosis/`, `app/missions/`, `app/pet/`, `app/community/`, `app/(auth)/`. 다른 사람 폴더는 건드리지 않는다.

### 브랜치

담당별 고정 브랜치를 쓰고 리뷰 없이 본인이 `main`에 머지한다(셀프 머지).

| 담당 | 브랜치 |
|---|---|
| A | `feat/diagnosis` |
| B | `feat/missions` |
| C | `feat/pet` |
| D | `feat/community` |
| E | `feat/infra` |

**하루 최소 1회 머지한다. 이틀 이상 묵힌 브랜치는 금지다.** 오래 묵은 브랜치는 머지 시점에 대형 충돌로 돌아오고, 이 팀에서 대형 충돌 해결은 기능 하나를 새로 만드는 것보다 오래 걸린다. 머지 절차와 금지 사항은 `CLAUDE.md` 4절에 있다.

### 세션 초기화 대비 문서

Claude 세션은 길어지면 초기화된다. 작업을 끝낼 때마다 `docs/dev/<기능>.md`에 현재 상태·구현 파일·결정 사항·막힌 것·다음 할 일을 갱신하고 `docs:` 커밋으로 남긴다. 형식은 `CLAUDE.md` 6절에 있다. 새 세션은 이 문서를 먼저 읽고 시작한다.

---

## 3. 일정

| 날짜 | 전원 | 개별 |
|---|---|---|
| 8/14 (목) | Prisma schema 초안 합의 | **E: 레포·Next.js 프로젝트·브랜치 5개 + `getCurrentUser()` 스텁 (최우선)** / A: 진단 문항 설계 + 미션 콘텐츠 착수 / B·C·D: clone 후 로컬 환경 + 화면 골격. C는 `calculateReward()` 먼저 |
| 8/15 (금) | **schema 확정**, 라이브 배포 첫 성공 | E: S3·CloudWatch·Bedrock 확인 완료 / A: **미션 41개 초안 완료 + 전원 검수** |
| 8/16 (토) | 하루 2회 통합 시작 | 기능 5개 병렬 착수 |
| 8/17 (일) | | 기능 개발 |
| 8/18 (월) | | 기능 개발 |
| 8/19 (화) | **중간 체크포인트** — 미완성 항목 목록화 | 기능 개발 |
| 8/20 (수) | 기능 동결 목표 | E: 발표 자료 착수 |
| 8/21 (목) | 통합·버그 수정 | 시드 데이터 + 데모 계정 3개 준비 |
| 8/22 (금) | **개발 마감**, 전체 플로우 점검 | |
| 8/23~24 (토·일) | 현직자 피드백 접수 | **잔여 기능 완성 우선** |
| 8/25 (월) | 잔여 기능 마무리 | 치명적 피드백만 반영 |
| 8/26 (화) | **녹화** — 기능별 클립 촬영 + 편집 | |
| 8/27 (수) | 발표 자료 마감, 리허설 | |
| 8/28 (목) | 발표 | |

---

## 4. 8/19 체크포인트 — 컷 순서

8/20 기능 동결이 어려워 보이면 아래 순서로 자른다. 그날 토론하지 않는다.

1. 커뮤니티 LLM 주제 추천 3개
2. ~~캐릭터 고유 효과를 씨앗 계열 1종으로 축소~~ — 해당 없음. 스킨 구조 변경(2026-08-20)으로 고유 효과 자체가 없어졌다(`effectType`은 전부 `NONE`). 자를 것이 남아 있지 않다
3. 방치형 자동 획득
4. 출석 보상 7일 캘린더

기능 개수는 유지하고 깊이만 얕게 만든다. 녹화 영상에 모든 기능이 들어가야 하므로 기능 자체를 통째로 없애지는 않는다.

---

## 5. 데모 계정 (8/21 준비)

유형 3개 각각 1개씩, 총 3개. 녹화용이므로 초기 상태가 아니라 "쓰던 계정"으로 보여야 한다.

각 계정 시드 내용:
- 펫 진화 2~3단계, 레벨 중간
- streak 12일, 출석 캘린더 진행 중
- 씨앗 넉넉히
- **별조각·친밀도 넉넉히** — 스킨 2500 / 배경 600은 정상 플레이로 36~39일이 걸린다. 상점 구매 장면(녹화 클립 5번)을 찍으려면 시드로 심어야 한다. 구매 전 잔액이 보이므로 딱 맞는 값이 아니라 넉넉한 값을 넣는다
- 치장 아이템 3~4개 획득 + 착용 상태
- 커뮤니티 글 5~6개와 댓글·좋아요가 붙은 상태
- 챗봇 대화 이력 몇 턴
- 단계 미션 1~2단계 완료, 3단계 진행 중

신규 가입 화면은 미션 0개·펫 1단계라 서비스가 초라해 보인다. 가입·진단 플로우만 신규 계정으로 찍고, 나머지는 데모 계정으로 찍는다.

---

## 6. 녹화 클립 목록 (8/26)

기능별로 개별 촬영해 편집한다. 막힌 구간만 다시 찍으면 되고, 클립별로 다른 데모 계정을 쓸 수 있다.

1. 가입 → 진단 6문항 → 결과(표시명·컬러) — 신규 계정, 답변 시나리오 사전 고정
2. 일일 미션 목록 → 완료 → 씨앗 획득
3. 단계 미션 3단계 → 사진 업로드 미션 달성
4. 펫 화면 → 씨앗 투입 → 진화 연출
5. 상점: 치장 구매(친밀도) → 착용 → 종족 외형 스킨 구매(별조각) → 전환. **고유 효과 표시는 없다** — 외형만 바뀐다
6. AI 챗봇 대화 (스트리밍)
7. 커뮤니티: 갤러리 → 글쓰기(주제 추천) → 댓글 → 좋아요
8. 출석 캘린더 + streak
9. AWS 콘솔 30초: Amplify 배포 로그, CloudWatch 대시보드, Cost Explorer

---

## 7. 리스크

**8/23~26을 잔여 기능 완성에 쓰기로 정했으므로 현직자 지적이 발표까지 남을 수 있다.** 현직자 지적은 심사위원 지적과 겹칠 가능성이 높다. 반영할 시간이 없더라도, 8/23에 받은 지적은 목록으로 정리해 "인지하고 있으며 이렇게 개선할 계획"이라는 슬라이드 한 장으로 만든다. 비용이 거의 들지 않고 방어력이 크다.

**총 작업량은 약 15인일이고 캐파 여유는 작다.** 8/19 체크포인트를 반드시 지키고, 미완성 항목이 3개를 넘으면 4절의 컷을 즉시 발동한다.
