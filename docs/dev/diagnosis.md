# 유형 진단 개발 문서 (담당 A)

세션이 초기화되면 이 문서를 먼저 읽는다. 작업을 끝낼 때마다 갱신하고 `docs:` 커밋으로 남긴다.
명세는 `SPEC.md` 2·3절, 규칙은 `CLAUDE.md`.

## 현재 상태
- 완료: 없음
- 진행 중: 진단 6문항 설계
- 미착수: 판정 함수, Bedrock 호출 2종, 결과 화면, 재진단, 스냅샷 테스트 18개, 미션 콘텐츠 41개

## 구현한 파일
- `lib/types.ts` — 종족·형용사 매핑, 기본 닉네임, 성장 곡선 상수 (골격 완료)

## 결정한 것과 이유
- 판정은 100% 코드. LLM은 질문 문장 다듬기와 자유 입력 enum 변환만 담당
- 형용사는 6번 문항 4지선다에 1:1 매핑. `ADJECTIVE_BY_CHOICE` 상수 테이블

## 막힌 것
- 없음

## 다음 할 일
- 6문항 선택지와 axis·weight 확정
- 미션 콘텐츠 41개 (8/15 마감, `prisma/seed/missions.ts`)
